import { CtrADirective } from './ctr-a.directive';

describe('CtrADirective', () => {
  it('should create an instance', () => {
    const directive = new CtrADirective();
    expect(directive).toBeTruthy();
  });
});
